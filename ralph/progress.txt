# Ralph Progress Log
Started: Sat Mar 14 22:30:07 -03 2026
---

## Codebase Patterns
- Item system follows a consistent pattern: constants → gameState array + nextSpawnDistance → spawn/update/render functions → bloom entry → update/render calls in playingState
- Spawn rarity is controlled by SPAWN_MIN/SPAWN_MAX constants (px traveled between spawns). Shield: 5000-7500px, Nitro: 2500-4000px
- All SFX use Web Audio API procedural synthesis in the AudioManager object — no audio files
- Collection uses AABB overlap between getPlayerHitbox() and item bounding box
- Float text feedback via spawnFloatText(x, y, text, color) on collection
- Bloom/glow added in renderBloom() function for each collectible type
- HUD bars (nitro, weapon) rendered at bottom-center of screen
- Bullet system: bullets[] array in gameState, updateBullets() handles spawn+move+collision, renderBullets() has phase-aware visuals
- Vehicle destruction uses spawnVehicleDestroyParticles() for fire+debris particle burst
- Bullet-vehicle collision uses same aabbOverlap() + getVehicleHitbox() as player collision
- Debug shortcuts in playingState: D=toggle debug, P=25k score, O=50k score, W=activate weapon
- Pause system: gamePaused flag in game loop skips fsm.update(); overlay renders on top of frozen frame
- Game loop is the place for global input handling (mute, pause) — runs before fsm.update()
- HUD icons/buttons: top-right for mute/pause, top-left for gyro toggle. Hit detection in canvas click + touchend handlers
- localStorage preferences: load on startup, save on change. Wrap in try/catch for private browsing
- New FSM states: declare DOM button variables early (line ~2406), create elements near other buttons (~2980), define state object before titleRankingState
- FSM state navigation from DOM buttons: use click handler with `if (fsm.currentState === targetState)` guard
- Touch scroll for overlay states: add case in touchend handler alongside titleRankingState pattern

---

## 2026-03-14 - US-001
- Implemented weapon power-up item: spawn, collect, HUD bar, SFX, bloom
- Added constants: WEAPON_ITEM_RADIUS, WEAPON_SPAWN_MIN/MAX (12000-20000px, 2-3x rarer than shield), WEAPON_DURATION (3s)
- Added shootingTimer state variable, player.shooting flag
- Timer stacks on re-collect (+3s added to remaining time)
- Files changed: src/main.js (+204 lines)
- **Learnings for future iterations:**
  - Follow the shield item as the closest pattern for new collectibles
  - gameState reset in resetGameState() must include new arrays, spawn distances, and player flags
  - Bloom entries go before "Player headlights" comment in renderBloom()
  - HUD bars go after renderNitroHUD(ctx) call in the render section
---

## 2026-03-14 - US-002
- Implemented automatic bullet firing system while weapon power-up is active
- Bullets fire every 200ms, move upward at 600px/s, max 10 on screen
- Phase-aware bullet visuals: metallic (road), blue missile (sky), green laser (space)
- AABB collision between bullets and traffic — 1 hit = 1 kill, vehicle removed + explosion particles
- +50 points per kill (× combo multiplier), increments combo counter
- Added AudioManager.playBulletShoot() (short square wave click) and AudioManager.playVehicleDestroy() (noise burst + low thump)
- Bloom entries for bullets (phase-dependent color glow)
- Added debug shortcut W to activate weapon in debug mode
- Files changed: src/main.js (~200 lines added), ralph/prd.json, ralph/progress.txt
- **Learnings for future iterations:**
  - Bullet system pattern: constants → gameState.bullets[] → updateBullets(dt) → renderBullets(ctx) → bloom entry
  - Vehicle destruction particles follow same pattern as explosion particles (fire + debris colors)
  - When adding new update functions, wire into playingState update() after item updates
  - When adding new render functions, wire into playingState render() between items and player layers
  - Debug shortcuts are gated behind `debugMode &&` check — safe to add for testing
---

## 2026-03-14 - US-003
- Implemented pause button system: ❚❚ icon in top-right, dark overlay with "PAUSADO" text and "Continuar" button
- P or Escape keys toggle pause on desktop; canvas tap on pause button icon or overlay for mobile
- Game loop skips fsm.update() when paused — all timers (shooting, nitro, shield, chromatic aberration) frozen
- Audio master gain reduced to 0.05 during pause, restored on resume (respects existing mute state)
- gamePaused reset in playingState.onExit() to prevent stale pause on game over transition
- Accumulator drained while paused to prevent burst of updates on resume
- Pause only available during playingState (not explosion/gameOver)
- P key coexists with debug shortcut (P=25k score in debugMode): non-debug P pauses, debug P passes through
- Canvas click handler: pause button area detection + overlay "Continuar" button + any-click-to-unpause
- Files changed: src/main.js (~100 lines added), ralph/prd.json, ralph/progress.txt
- **Learnings for future iterations:**
  - Game loop (gameLoop function) is the right place for global input that should work across states or override state behavior
  - When pausing, drain the accumulator (set to 0) to prevent time-burst on resume
  - AudioManager.masterGain.gain.setTargetAtTime() is the correct way to fade audio without clicks
  - Canvas click detection pattern: convert client coords to logical coords via getBoundingClientRect + scale factors
  - playingState.onExit() must clean up any global flags (gamePaused) to prevent stale state
- Gyroscope: DeviceOrientationEvent.requestPermission() needed for iOS 13+, must be called from user gesture
- Gyroscope: detach listener in onExit() but keep gyroEnabled=true so it auto-restarts in next onEnter()
---

## 2026-03-14 - US-004
- Implemented gyroscope base control system for mobile devices
- DeviceOrientationEvent with iOS 13+ requestPermission() support
- Dead zone of ±5°, max tilt angle 35°, smooth lerp interpolation (0.15 factor)
- Gyroscope off by default — activated via toggleGyroscope() function
- Arrow touch buttons hidden when gyro active, restored when deactivated
- Gyro listener detached in playingState.onExit(), re-attached in onEnter() to ensure no processing during menus/game over
- Camera roll maps from gyro gamma for visual tilt feedback
- Files changed: src/main.js (~120 lines added), ralph/prd.json, ralph/progress.txt
- **Learnings for future iterations:**
  - iOS 13+ requires DeviceOrientationEvent.requestPermission() called from a user gesture (tap) — async
  - Gyro gamma range is -90 to +90 degrees; effective range for game is much smaller (~35°)
  - Lerp factor of 0.15 provides smooth movement without feeling sluggish
  - When detaching listener in onExit, keep the enabled flag so state can auto-restore on re-enter
  - toggleGyroscope() is the public API — US-005 will wire this to a UI button and sensitivity slider
---

## 2026-03-14 - US-005
- Implemented gyroscope toggle button + sensitivity slider HUD on canvas (mobile only)
- Toggle button: tilted phone icon at top-left, green when active, white/dim when inactive
- Sensitivity slider: horizontal track with 7 levels (0.5x to 2.0x), appears when gyro is on
- Slider uses tap-to-select: user taps on slider position to change sensitivity level
- Preferences saved/restored via localStorage with key 'roadrush_gyro' (enabled + sensitivityIndex)
- On fresh load with saved prefs, auto-requests iOS permission in playingState.onEnter (user gesture context)
- Changed GYRO_SENSITIVITY from const to mutable gyroSensitivity, wired to slider
- Hit detection added to both canvas click and touchend handlers
- Files changed: src/main.js (~120 lines added), ralph/prd.json, ralph/progress.txt
- **Learnings for future iterations:**
  - Canvas HUD buttons: render in gameLoop after fsm.render(), hit-test in both click and touchend handlers
  - For mobile-only UI, guard with `if (!isMobileDevice) return` in render and `if (!isMobileDevice) return false` in hit detection
  - localStorage: always wrap in try/catch — private browsing mode may throw
  - When restoring preferences from localStorage, validate values are in expected range (e.g., clamp index)
  - iOS gyro permission from localStorage restore: must be re-requested each page load since it's session-scoped
---

## 2026-03-14 - US-006
- Implemented communityFeaturesState FSM state with full canvas-rendered feature list
- 5 community features displayed in styled cards: name, requester, description, status badge
- Status badges: green "✅ Implementada" for done, yellow "🔜 Em breve" for upcoming
- Features: Power-up de Tiro (S), Controle por Giroscópio (John Snow), Botão de Pause (Comunidade), Novos Power-ups (soon), Modo Multiplayer (soon)
- DOM buttons: VOLTAR (returns to title), Sugerir Feature (opens existing feedback modal)
- Changed feedback button on title screen to navigate to communityFeaturesState instead of opening modal directly
- Scroll support: arrow keys on desktop, touch swipe on mobile, scroll indicator bar
- ESC key also returns to title screen
- Design: dark gradient bg, purple accent colors, grid lines, rounded card borders — consistent with titleRankingState style
- Files changed: src/main.js (~256 lines added), ralph/prd.json, ralph/progress.txt
- **Learnings for future iterations:**
  - New FSM overlay states follow pattern: declare DOM vars early → create elements with positioning fn → define state object → wire touch/click handlers
  - When repurposing an existing button (feedbackBtn), check all states where it's shown — use fsm.currentState guard in handler to differentiate behavior
  - Canvas clip regions (ctx.save/clip/restore) are essential for scrollable content areas
  - Rounded rect drawing requires manual quadraticCurveTo path — no built-in Canvas API for it
  - DOM buttons positioned via canvas.getBoundingClientRect() + logical-to-screen scale factor
---
