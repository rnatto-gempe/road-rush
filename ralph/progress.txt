## Codebase Patterns
- Road rendering is in `renderRoad(ctx, scrollOffset)` — rumble strips and lane dashes both use scrollOffset modulo their period to compute starting y position
- To scroll visuals top-to-bottom (toward player), use `y = offset - PERIOD` (not `y = -offset`)
- Game states are objects with `onEnter()`, `onExit()`, `update(dt)`, `render(ctx)` methods managed by `fsm`
- gameOverState.render() is fully independent of renderRoad() — no scroll-related regression risk when modifying road rendering
- Opponents spawn at `y = -type.height` (above screen) and move down via `v.y += visualSpeed * dt`
- Speed lines already scroll correctly (positive offset direction via modulo wrap)
- FEEDBACK_WEBHOOK_URL constant was pre-added to main.js (needed for US-006)
- Opponent visual speed formula: `visualSpeed = getEffectiveScrollSpeed() * (1 - speedRatio)` — always positive, computed dynamically each frame (not stored at spawn)
- Game state variables (fsm, gameOverState, etc.) are module-scoped, NOT accessible from Playwright evaluate() — browser testing limited to visual screenshots
- Avoid long `sleep` between Playwright actions — browser context closes; navigate+act+screenshot must be done in rapid sequence
- `renderRankingPanel(ctx, rankingData, rankingStatus, rankingScroll, rankingDotTime, highlightName)` is now a shared function at line ~4486 — use it for any new state that needs a ranking panel

# Ralph Progress Log
Started: Sat Mar 14 16:57:25 -03 2026
---

## 2026-03-14 17:05 - US-001
- Reversed lane dash and rumble strip scroll direction from bottom-to-top to top-to-bottom
- Changed rumble strip offset: `y = -rumbleOffset` → `y = rumbleOffset - RUMBLE_PERIOD`
- Changed dash offset: `-dashScrollOffset` → `dashScrollOffset - DASH_PERIOD`
- Files changed: src/main.js
- **Learnings for future iterations:**
  - The offset math is: `y = offset - PERIOD` makes visuals scroll downward (toward player) as scrollOffset increases
  - gameOverState rendering is completely independent of renderRoad — no regression risk
  - Browser testing with Playwright is flaky due to context closures on long waits — use short evaluate calls
  - scrollOffset increases monotonically: `effSpeed * dt` where effSpeed is always positive
---

## 2026-03-14 - US-002
- Changed opponent ownSpeed from static (set at spawn) to dynamic (recomputed every frame): `v.ownSpeed = getEffectiveScrollSpeed() * type.speedRatio`
- Changed visualSpeed formula: `getEffectiveScrollSpeed() - v.ownSpeed` → `getEffectiveScrollSpeed() * (1 - type.speedRatio)` — ensures always-positive (downward) displacement
- Fixed `relativeVy` in collision handler to use same formula: `getEffectiveScrollSpeed() * (1 - speedRatio)`
- Files changed: src/main.js
- **Learnings for future iterations:**
  - Game state variables (fsm, gameOverState, etc.) are module-scoped — cannot be accessed via Playwright evaluate(); browser testing is limited to visual screenshots
  - Avoid `sleep` between Playwright calls — browser context closes; navigate+act+screenshot must run in rapid sequence without long pauses
  - gameOverState is independent of traffic/speed physics — speed formula changes carry zero game over regression risk
  - ownSpeed was previously set once at spawn time and became stale as scrollSpeed ramped up; now recomputed each frame for correct dynamic behavior
---

## 2026-03-14 - US-003
- Extracted ranking rendering logic from gameOverState.render() into standalone `renderRankingPanel(ctx, rankingData, rankingStatus, rankingScroll, rankingDotTime, highlightName)` function
- gameOverState.render() now delegates to renderRankingPanel, passing `nameInputEl.value.trim()` as highlightName
- Visual output is identical — same RANK_Y=537, row heights, colors, scroll indicators
- Files changed: src/main.js
- **Learnings for future iterations:**
  - renderRankingPanel is defined just before `// --- GameOver State ---` — any new state (e.g. titleRankingState) can call it directly
  - highlightName replaces the old `nameInputEl.value.trim()` inline lookup — callers supply the name string
  - Build passes with `npm run build`; title screen renders correctly via Playwright
---
