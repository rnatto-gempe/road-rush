## Codebase Patterns
- FSM states: titleState, playingState, explosionState, gameOverState — all have onEnter/onExit/update/render
- titleState (not startState) is the initial state — "Press Enter to Start" transitions to playingState
- `keys` object is global: set keys['ArrowLeft']/keys['ArrowRight'] to control player movement
- `resizeCanvas()` computes displayWidth/displayHeight via letterbox math — canvas is centered using flexbox
- Touch button divs must use `position:fixed` (not absolute) since they overlay the full viewport
- `{ passive: false }` required on touch event listeners to allow e.preventDefault()
- Touch button positioning: left = (innerWidth - displayWidth) / 2, top = (innerHeight - displayHeight) / 2 + displayHeight * 0.80

# Ralph Progress Log
Started: Fri Mar 13 23:50:56 -03 2026
---

## 2026-03-13 - US-001
- Implemented two touch button divs (.touch-left, .touch-right) appended to document.body
- Positioned via `positionTouchButtons()` called from `resizeCanvas()` — bottom 20% of canvas display area, 50/50 split
- Touch event handlers (touchstart/touchmove/touchend/touchcancel) wire into keys['ArrowLeft'] / keys['ArrowRight']
- `{ passive: false }` + `e.preventDefault()` on all events to suppress scroll/zoom
- `touch-action: none` on both divs via inline CSS
- Buttons shown in playingState.onEnter, hidden (and keys cleared) in playingState.onExit
- Browser verified: buttons are `display:block` during playingState, correctly positioned
- Files changed: src/main.js, ralph/prd.json, ralph/progress.txt
- **Learnings for future iterations:**
  - The canvas uses CSS flexbox centering; touch overlays need `position:fixed` with computed offsets
  - `keys` is just a plain object — any code can read/write keys['ArrowLeft'] etc.
  - playingState.onExit must also reset keys to false to prevent stuck-key on state transitions
---
