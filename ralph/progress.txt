# Ralph Progress Log
Started: Fri Mar 13 18:22:04 -03 2026
---

## Codebase Patterns
- AudioManager is a module-scope singleton in src/main.js — init() creates AudioContext, resume() handles autoplay policy
- All audio nodes must connect through AudioManager.masterGain for global mute support
- Use gain envelopes (0.01s attack/release) on all oscillators/noise to avoid clicks
- Use oscillator.onended / source.onended to disconnect nodes and prevent memory leaks
- AudioContext is created on first Enter press in titleState (browser autoplay policy compliance)
- The game has two places where Enter starts gameplay: titleState and gameOverState — both need audio handling
- FSM calls onExit() automatically if defined — use it for cleanup when leaving a state (covers all exit paths)
- Long-lived audio nodes (engine, road, nitro) use start/stop/update pattern: start in onEnter, stop in onExit, update per frame
- Nitro audio activation is driven by nitroTimer in playingState.update, not in updateNitroItems — keeps audio logic centralized
- Reuse noise buffers (store on AudioManager) when creating looping noise sources to avoid repeated buffer generation
- exponentialRampToValueAtTime cannot ramp to 0 — use 0.001 as the floor for all decay envelopes
- Shield break has TWO trigger locations: handleVehicleCollision and border collision — both need consistent SFX
- Pickup SFX gains (0.25-0.35) sit below collision/explosion hierarchy (0.4-0.8) — maintain this for new SFX
- Coin cluster ascending pitch uses AudioManager.coinSeq with a 0.4s timeout, decremented via updateCoinSeq(dt) in game loop
- Near miss/combo/bonus SFX gains (0.2-0.3) sit at or below pickup SFX hierarchy — maintain volume ordering: explosion > collision > pickups >= bonus SFX
- triggerNearMiss() is the single trigger point for near miss whoosh + combo stinger — no need to wire multiple locations
- Overtake bonus SFX is in the traffic filter callback (v.y > 760 && !v.collided) — fires frequently, keep it subtle

---

## 2026-03-13 18:23 - US-001
- Implemented AudioManager object with AudioContext, masterGain, muted state, toggleMute()
- Added playTone(freq, duration, type, gainValue) helper with gain envelope and auto-disconnect
- Added createNoise(duration, filterFreq, filterType) helper with filter, envelope, auto-disconnect
- AudioContext init/resume on first Enter press in titleState
- Files changed: src/main.js
- **Learnings for future iterations:**
  - AudioManager is module-scoped (not on window), so browser console can't access it directly — use evaluate with module context or add temp debug
  - The titleState and gameOverState both have Enter key handlers — future audio state transitions need to handle both paths
  - Build requires `npm install` first if node_modules is missing
  - No console errors after AudioManager integration — Web Audio API is well-supported
---

## 2026-03-13 21:27 - US-002
- Wired engine drone to playingState lifecycle: startEngine() in onEnter, stopEngine() in onExit, updateEngine(scrollSpeed) every frame
- Engine methods (startEngine, stopEngine, updateEngine) were already implemented in US-001 AudioManager
- Added onExit() to playingState — FSM already supports it, covers both explosion and fuel-empty exit paths
- Files changed: src/main.js
- **Learnings for future iterations:**
  - The FSM calls onExit() automatically if defined on the current state — use this for cleanup
  - playingState has two exit paths: collision → explosionState, fuel empty → gameOverState; onExit covers both
  - Engine drone uses long-lived nodes with setTargetAtTime for smooth parameter changes — no per-frame node creation
---

## 2026-03-13 - US-003
- Added road ambience: looping white noise through band-pass filter (~300-800Hz), volume/bandwidth scale with scrollSpeed
- Added nitro boost audio: high-freq sawtooth harmonic at 2x engine freq with own gain node, plus 1.3x engine pitch boost via updateEngine
- Road noise starts/stops with engine in playingState onEnter/onExit
- Nitro audio activates/deactivates based on nitroTimer in playingState.update
- Files changed: src/main.js
- **Learnings for future iterations:**
  - Modifying updateEngine to check nitro.active flag is cleaner than manually setting pitch in startNitro — avoids fighting with per-frame updates
  - Noise buffer can be cached on AudioManager (road.buffer) and reused across start/stop cycles
  - stopNitro must be called before stopEngine in onExit to avoid accessing dead engine nodes
  - The nitro harmonic needs per-frame updateNitro() calls to track the boosted engine frequency
---

## 2026-03-13 - US-004
- Added playCrash() SFX: high-pass noise burst (metallic clang) + low sine thump at 60Hz, both 0.15s with exponential decay
- Added playExplosion() SFX: low sine sweep 80→30Hz over 0.5s + noise burst with decaying LP filter over 0.8s + 4 random crackle bursts over 1.5s
- Added playShieldBreak() SFX: sine sweep 2kHz→500Hz over 0.2s + short high-pass noise burst
- Wired playCrash() into handleVehicleCollision (fatal collision path)
- Wired playExplosion() into explosionState.onEnter()
- Wired playShieldBreak() into both shield break locations (vehicle collision + border collision)
- Files changed: src/main.js
- **Learnings for future iterations:**
  - There are TWO shield break locations: handleVehicleCollision (line ~850) and border collision check (line ~1109) — both need SFX
  - Explosion is the loudest event (gain 0.8) vs crash/shield (0.4-0.5) — maintain this hierarchy for future SFX
  - exponentialRampToValueAtTime cannot ramp to 0 (must use 0.001) — use this for all decay envelopes
  - Crackle bursts use scheduled start times (now + delay) with gain automation starting at the same offset
---

## 2026-03-13 - US-005
- Added playFuelPickup() SFX: ascending sine 400→800Hz over 0.15s with soft envelope
- Added playCoinPickup() SFX: triangle wave ding at 1200Hz+, pitch rises +50Hz per rapid coin in cluster via coinSeq counter
- Added playNitroPickup() SFX: aggressive sawtooth sweep 200→600Hz over 0.3s
- Added playShieldPickup() SFX: two detuned sines at 500Hz/505Hz creating 5Hz beat frequency, 0.4s with slow fade-in
- Added updateCoinSeq(dt) to decay the cluster sequence counter after 0.4s gap
- Wired all 4 SFX into their respective update functions (updateFuelItems, updateCoins, updateNitroItems, updateShieldItems)
- Files changed: src/main.js
- **Learnings for future iterations:**
  - Coin cluster ascending pitch uses a sequence counter (coinSeq) with a 0.4s timeout — reset timer is decremented in the game update loop
  - Shield pickup plays SFX even when player already has shield (item still gets collected with anim) — this is intentional for feedback
  - Pickup SFX gains are in the 0.25-0.35 range to sit below collision/explosion hierarchy
---

## 2026-03-13 - US-006
- Added playNearMiss() SFX: band-passed noise burst (~2kHz center, Q 1.5) for 0.1s whoosh
- Added playComboUp(combo) SFX: ascending sine stinger at 600Hz + combo×100Hz, 0.15s — triggers at x2+
- Added playSurvivorBonus() SFX: major chord with three sines at 400/500/600Hz, 0.3s gentle decay
- Added playOvertake() SFX: subtle low-pass filtered sawtooth sweep 80→120Hz, 0.15s
- Wired near miss whoosh + combo stinger into triggerNearMiss()
- Wired survivor bonus chord into survivor bonus scoring block in playingState.update
- Wired overtake vroom into traffic removal filter (clean overtake path)
- Files changed: src/main.js
- **Learnings for future iterations:**
  - triggerNearMiss() is the single trigger point for all near miss audio — no need to wire multiple locations
  - Overtake bonus fires for every clean vehicle pass — keep its SFX very subtle (gain 0.2) to avoid fatigue
  - Combo stinger uses the already-incremented comboMultiplier value, so at first near miss (combo goes 1→2) it plays at freq 800Hz
  - Volume hierarchy: explosion (0.8) > collision (0.5) > pickups (0.25-0.35) >= bonus SFX (0.2-0.3)
---
